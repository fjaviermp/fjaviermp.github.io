# -----------------------------
# Sitemap / Robots (build step)
# -----------------------------
# Dominio final en producción (sin slash final).
SITE_URL=https://fjaviermp.com

# Carpeta de salida del build de Vite.
DIST_DIR=dist

# Ruta al router para leer las rutas y generar el sitemap automáticamente.
ROUTER_FILE=src/router/index.ts